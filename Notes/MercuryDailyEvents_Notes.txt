# Clean  MercuryDailyEvents

# Parameters:
LS_COLS = [
	Date	CompanyCode	CompanyName	Country	Operation	EventType	DeviceCategory	SearchTerm	ContentType	ContentTitle	DownloadLanguage	Theme	Route	UniqueUsers	DateRange
]

LS_STR_COLS = [
	CompanyCode	CompanyName	Country	Operation	EventType	DeviceCategory	SearchTerm	ContentType	ContentTitle	DownloadLanguage	Theme	Route
]

LS_INT_COLS = [
	UniqueUsers
]

SORT_ORDER = [
	'Date', 'CompanyCode', 'CompanyName', 'Country', 'Operation', 'EventType', 'DeviceCategory', 'SearchTerm', 	'ContentType', 'ContentTitle'
]


[x] Ingest the `MercuryDailyEvents_yyyy-mm-dd.txt` data into a Pandas Dataframe.
    The file uses a semicolon ; as the field delimiter.
    Read with "utf-8" encoding.

[x] Rename Columns as follows:
    ContentTitleEN -> ContentTitle
    downloadLanguage -> DownloadLanguage
    uniqueUsers -> UniqueUsers


[x] Locate the input file `MercuryDailyEvents_yyyy-mm-dd.txt"

[x] Derive the `month-tag` from the filename, and subtract 1 month from it.  Store the result inside `year`, `month` variables respectively.
    Concatenate `year` and `month` into a single string: `month_tag` = <year><month>.

[x] The Data Columns need to be arranged in the order specified in LS_COLS. Drop any other columns that are not contained in `LS_COLS`.

[x] Convert `Date` entries to the following format: ("%Y-%m-%d %H:%M:%S.%f").str[:-3]
	Example output for this would look like:  "2026-07-13 00:00:00.000"

[x] Strip any HTML tags, entities, HTML attributes from the `EventSection` column.

[x] Trim any redundant white space (trailing space, leading space, double spaces, tabs), .str.strip(), in the string fields specified in LS_STR_COLS.  Please also collapse any Double white space into a single white space for the fields specified in LS_STR_COLS.
```
for col in LS_STRING_COLS:
    df[col] = df[col].str.strip().str.replace(r"\s+", " ", regex=True)
```

[x] Remove any rows that are completely blank.

[x] Remove any rows where the `Date` entry is blank/missing/NULL.

[x] Cast the columns specified in LS_INT_COLS to integer format.

[x] Sort the rows in Ascending (or A->Z) order based on columns specified in `SORT_ORDER`.


# Save the dataset
Look at the filename of the input data.

Save and store the dataset as: MercuryDailyEvents_<month_tag>_cleaned.csv
So, as an example, the input of MercuryDailyEvents_2026-08-02.txt would lead to the output: MercuryDailyEvents_202607_cleaned.csv.
Use ";" as the field delimiter.

[x] Write a Summary Report.